# TODO: analyse_ticker() — takes ticker + price + news, returns structured analysis
# TODO: generate_earnings_brief() — pre-earnings summary for a single stock
# TODO: generate_daily_digest() — end-of-run market summary across all tickers
